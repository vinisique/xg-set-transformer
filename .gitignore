# Ambiente virtual — nunca versionar (contém o PyTorch inteiro, ~2 GB)
poc/.venv/
.venv/
venv/

# Dados brutos e derivados — pesados e reconstrutíveis pelos scripts de download
*.npz
*.sqlite
poc/data/

# Saídas de experimentos: os artefatos ficam fora do git,
# mas docs/experimentos/RESULTS.md (o log com os números) é versionado.
experiments/
*.pt
*.pth
*.ckpt

# Material de terceiros — NÃO vai para o repositório PÚBLICO.
# Slides, notebooks e enunciado são de autoria do Prof. Ronaldo Prati / UFABC, e o
# texto do feedback são palavras dele. Republicar num repositório público seria
# redistribuir material de terceiros sem permissão.
# Tudo continua normalmente no seu disco — apenas fora do git.
# Com autorização do professor, basta remover as linhas abaixo.
aulas/
project-assignment.pdf
feedback_proposta_entrega1.md

# Config local do editor/ferramenta — não interessa a quem clona
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Sistema operacional / editores
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Template SBC bruto (zip extraido) — os arquivos necessarios ja estao
# copiados em relatorio/ (sbc-template.sty e sbc.bst)
modelosparapublicaodeartigos/

# Artefatos de compilacao LaTeX
*.aux
*.bbl
*.blg
*.log
*.out
*.dvi
*.synctex.gz

# PDFs baixados do Overleaf caem na raiz com nome gerado pelo titulo do artigo.
# A versao entregue e relatorio/artigo.pdf; manter copias soltas confunde quem
# clona, porque nao da para saber qual e a atual.
O_jogador_como_token*.pdf

# PDF da proposta gerado por gera_proposta_pdf.py a partir de
# proposta_entrega1.md. E byte a byte igual ao Proposta_entrega1_enviado.pdf,
# que fica versionado porque o nome registra qual documento foi submetido.
proposta_entrega1.pdf
